language: java

jdk:
   - oraclejdk8

before_script: 
  - mkdir -p shippable/testresults
  - mkdir -p shippable/codecoverage

script:
  - mvn clean cobertura:cobertura -Dcobertura.report.format=xml
  - mvn test
  
after_script:
  - cp ws-simulator-service/target/site/cobertura/* shippable/codecoverage
  - cp ws-simulator-service/target/surefire-reports/* shippable/testresults